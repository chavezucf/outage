name: Generate Incident RSS Feed

on:
  push:
    paths:
      - 'data/incidents/*.json'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Generate RSS feed
        run: |
          echo '<!-- Placeholder for RSS generation script -->'
          # TODO: Replace with script to generate data/incidents.rss from data/incidents/*.json
      - name: Commit RSS feed
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add data/incidents.rss
          git commit -m 'Auto-generate incidents RSS feed' || echo 'No changes to commit'
          git push
